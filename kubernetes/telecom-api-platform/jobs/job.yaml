apiVersion: batch/v1
kind: Job

metadata:
  name: print-hello
  namespace: telecom

spec:
  template:
    spec:
      restartPolicy: Never

      containers:
      - name: hello
        image: busybox
        command:
        - echo
        - "Hello from Kubernetes Job"
